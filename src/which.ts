import {
    observable, 
    makeAutoObservable,
    makeObservable,
    action,
    computed
} from 'mobx'
import {observer} from 'mobx-react-lite'


export {
    // mobx
    makeAutoObservable,
    observable,
    makeObservable,
    action,
    computed,

    // mobx-react-lite
    observer,
}