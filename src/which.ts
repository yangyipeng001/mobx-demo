import {
    observable, 
    makeAutoObservable,
    makeObservable,
    action
} from 'mobx'
import {observer} from 'mobx-react-lite'


export {
    // mobx
    makeAutoObservable,
    observable,
    makeObservable,
    action,

    // mobx-react-lite
    observer,
}